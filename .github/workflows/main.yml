name: RDP Setup and Software Installation

on:
  workflow_dispatch:

jobs:
  setup-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
                             -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "SecurityLayer" -Value 0 -Force

          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389
          Restart-Service -Name TermService -Force

      - name: Create RDP User with Fixed Password
        run: |
          $password = 'Zain.rty123'
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force

          if (Get-LocalUser -Name "Administrator" -ErrorAction SilentlyContinue) {
              Set-LocalUser -Name "Administrator" -Password $securePass
          } else {
              New-LocalUser -Name "Administrator" -Password $securePass -AccountNeverExpires
          }

          if (-not (Get-LocalGroupMember -Group "Administrators" -Member "Administrator" -ErrorAction SilentlyContinue)) {
              Add-LocalGroupMember -Group "Administrators" -Member "Administrator"
          }
          if (-not (Get-LocalGroupMember -Group "Remote Desktop Users" -Member "Administrator" -ErrorAction SilentlyContinue)) {
              Add-LocalGroupMember -Group "Remote Desktop Users" -Member "Administrator"
          }

          echo "RDP_CREDS=User: Administrator | Password: $password" >> $env:GITHUB_ENV

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        run: |
          $authKey = "${{ secrets.TAILSCALE_AUTH_KEY }}"
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=$authKey --hostname=gh-runner-$env:GITHUB_RUN_ID

          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep -Seconds 5
              $retries++
          }
          if (-not $tsIP) {
              Write-Error "Tailscale IP not assigned. Exiting."
              exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Verify RDP Accessibility
        run: |
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389
          if (-not $testResult.TcpTestSucceeded) {
              Write-Error "TCP connection to RDP port 3389 failed"
              exit 1
          }
          Write-Host "TCP connectivity successful!"

      - name: Ensure Administrator Profile & Desktop (Robocopy)
        run: |
          $adminProfile = "C:\Users\Administrator"
          $adminDesktop = "$adminProfile\Desktop"

          if (-not (Test-Path $adminProfile)) {
              Write-Host "Administrator profile missing — creating from Default..."
              robocopy "C:\Users\Default" $adminProfile /MIR /XD "AppData\Local\Application Data" /MT:4
              icacls $adminProfile /grant "Administrator:(OI)(CI)F" /T | Out-Null
          } else {
              Write-Host "Administrator profile already exists."
          }

          if (-not (Test-Path $adminDesktop)) { New-Item -ItemType Directory -Path $adminDesktop -Force | Out-Null }

      - name: Download, Extract and Deploy sOFTWARES Contents
        run: |
          $downloadUrl = "https://github.com/Zaiinalii/RDP/releases/download/v1/sOFTWARES.rar"
          $downloadPath = Join-Path $env:TEMP "sOFTWARES.rar"
          $extractPath = Join-Path $env:TEMP "sOFTWARES_extracted"
          $adminDesktop = "C:\Users\Administrator\Desktop"

          Invoke-WebRequest -Uri $downloadUrl -OutFile $downloadPath -UseBasicParsing -ErrorAction Stop

          $sevenZipExe = "C:\Program Files\7-Zip\7z.exe"
          if (-not (Test-Path $sevenZipExe)) { choco install 7zip -y; Start-Sleep -Seconds 10 }

          & "$sevenZipExe" x "$downloadPath" "-o$extractPath" -y

          $innerFolder = Get-ChildItem -Path $extractPath | Where-Object { $_.PSIsContainer } | Select-Object -First 1
          Get-ChildItem -Path $innerFolder.FullName | ForEach-Object {
              $dest = Join-Path $adminDesktop $_.Name
              if ($_.PSIsContainer) { Copy-Item $_.FullName $dest -Recurse -Force } else { Copy-Item $_.FullName $dest -Force }
          }

          Remove-Item $downloadPath -Force
          Remove-Item $extractPath -Recurse -Force

      - name: Download & Install Software Officially
        run: |
          $adminDesktop = "C:\Users\Administrator\Desktop"

          # Download VS Code Installer
          $vscodeUrl = "https://update.code.visualstudio.com/latest/win32-x64-user/stable"
          $vscodeInstaller = Join-Path $adminDesktop "VSCodeUserSetup-x64.exe"
          Invoke-WebRequest -Uri $vscodeUrl -OutFile $vscodeInstaller
          Start-Process -FilePath $vscodeInstaller -ArgumentList "/verysilent /mergetasks=!runcode /norestart" -Wait

          # Download PIA Installer
          $piaUrl = "https://www.privateinternetaccess.com/installer/pia-windows-x64-latest.exe"
          $piaInstaller = Join-Path $adminDesktop "pia-windows-x64.exe"
          Invoke-WebRequest -Uri $piaUrl -OutFile $piaInstaller
          Start-Process -FilePath $piaInstaller -ArgumentList "/S /norestart" -Wait

          # Download Python Installer (example Python 3.12)
          $pythonUrl = "https://www.python.org/ftp/python/3.12.2/python-3.12.2-amd64.exe"
          $pythonInstaller = Join-Path $adminDesktop "python-3.12.2-amd64.exe"
          Invoke-WebRequest -Uri $pythonUrl -OutFile $pythonInstaller
          Start-Process -FilePath $pythonInstaller -ArgumentList "/quiet InstallAllUsers=1 PrependPath=1 Include_pip=1 /norestart" -Wait

      - name: Verify Installed Software
        run: |
          # VS Code
          if (Test-Path "C:\Users\Administrator\AppData\Local\Programs\Microsoft VS Code\Code.exe") {
              Write-Host "VS Code installed successfully."
          } else { Write-Error "VS Code installation FAILED!" }

          # PIA
          if (Test-Path "C:\Program Files\Private Internet Access\pia.exe") {
              Write-Host "PIA installed successfully."
          } else { Write-Error "PIA installation FAILED!" }

          # Python
          if (Get-Command python -ErrorAction SilentlyContinue) {
              Write-Host "Python installed successfully."
          } else { Write-Error "Python installation FAILED!" }

      - name: Maintain RDP Connection
        run: |
          Write-Host "`n=== RDP ACCESS ==="
          Write-Host "Address: $env:TAILSCALE_IP"
          Write-Host "Username: Administrator"
          Write-Host "Password: $env:RDP_CREDS"
          Write-Host "==================`n"
          while ($true) {
              Write-Host "[$(Get-Date)] RDP Active"
              Start-Sleep -Seconds 300
          }
